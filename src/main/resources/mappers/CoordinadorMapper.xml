<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.projectKepler.persistence.mybatis.mappers.CoordinadorMapper">
    <select parameterType="map" id="loadCoordinador" resultMap="CoordinadorResult"> 
        select

            nombre,
            codigo

        FROM  CoordinadorCancelaciones
    </select>
    
    <resultMap type='CoordinadorCancelaciones' id='CoordinadorResult' >
        <id property='codigo' column='codigo'/> 
        <result property='nombre' column='nombre'/>
    </resultMap>
</mapper>